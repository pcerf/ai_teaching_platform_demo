version: '3.8'
services:
  ki-quiz:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - APP_ID=${APP_ID}
      - APP_TOKEN=${APP_TOKEN}
      - AI_SERVICE_URL=${AI_SERVICE_URL}
      - AI_BASE_URL=${AI_BASE_URL}
      - AI_API_KEY=${AI_API_KEY}
      - AI_MODEL=${AI_MODEL}
      - AUTH_UPSTREAM=${AUTH_UPSTREAM}
    networks:
      - coolify
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=coolify"
      - "traefik.http.routers.ki-quiz.rule=Host(`ki-quiz.hub.lumentic.de`)"
      - "traefik.http.routers.ki-quiz.tls.certresolver=letsencrypt"
      - "traefik.http.routers.ki-quiz.entrypoints=https"
      - "traefik.http.routers.ki-quiz.middlewares=forward-auth@file"
      - "traefik.http.routers.ki-quiz.service=ki-quiz"
      - "traefik.http.services.ki-quiz.loadbalancer.server.port=5000"

networks:
  coolify:
    external: true
