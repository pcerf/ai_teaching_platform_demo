version: '3.8'
services:
  ki-quiz-test7:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - APP_ID=${APP_ID}
      - APP_TOKEN=${APP_TOKEN}
      - AI_SERVICE_URL=${AI_SERVICE_URL}
      - AI_BASE_URL=${AI_BASE_URL}
      - AI_API_KEY=${AI_API_KEY}
      - AI_MODEL=${AI_MODEL}
      - AUTH_UPSTREAM=${AUTH_UPSTREAM}
    networks:
      - coolify
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=coolify"
      - "traefik.http.routers.ki-quiz-test7.rule=Host(`ki-quiz-test7.hub.lumentic.de`)"
      - "traefik.http.routers.ki-quiz-test7.tls.certresolver=letsencrypt"
      - "traefik.http.routers.ki-quiz-test7.entrypoints=https"
      - "traefik.http.routers.ki-quiz-test7.middlewares=forward-auth@file"
      - "traefik.http.routers.ki-quiz-test7.service=ki-quiz-test7"
      - "traefik.http.services.ki-quiz-test7.loadbalancer.server.port=5000"

networks:
  coolify:
    external: true
